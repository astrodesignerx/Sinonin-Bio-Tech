# The website is the root package; the Sanity Studio is a separate one so its
# bundler and dependencies stay out of the site's build.
packages:
  - studio

allowBuilds:
  sharp: true
  esbuild: true
  unrs-resolver: true
  '@parcel/watcher': true
  '@swc/core': true
